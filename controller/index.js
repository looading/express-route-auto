
module.exports = function(app) {

  // app.use(session({
  //   resave: false,
  //   saveUninitialized: true,
  //   secret: 'keyboard cat',
  //   cookie: { secure: true }
  // }))

  // app.use(generate());
}
